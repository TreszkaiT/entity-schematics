{
    "$schema": "http://json-schema.org/schema",
    "$id": "entity-authentication.json",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "name"
        },
        "apiPath": {
            "type": "string",
            "description": "Location of api folder. Relative from project root."
        }
    },
    "required": ["name", "apiPath"]
}
